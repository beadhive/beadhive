<!-- ws:agf:start (managed by `ws rig init` — edit outside these markers; `-f` refreshes) -->
## AGF — Agentic Git Flow

This repo is onboarded as a **`ws` rig** and develops via **AGF**: work is tracked in beads
and driven through `ws`, **not** raw `git` / `bd` / `gh`.

- **Is this repo set up for AGF?** → run `ws rig ready` (add `-v` for the line-item breakdown).
- **Lifecycle, roles, conventions:** see `.beads/PRIME.md` and `docs/AGF.md`.
- Drive beads with `ws work`; load the role skill for your seat (coordinator / developer / merger).
<!-- ws:agf:end -->
