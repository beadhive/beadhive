# Agents and Herdr reference-extraction evidence

Status: measured reference-module extraction

Evidence date: 2026-09-01 UTC

Before revision: `5e4a76700e85b4bdda97640eeec5c2bbac632b46`

Measured code revision: `d0e1dfa2aeb50ad90cfaf8c03fd9f74fda15956b`

The before revision is the characterized source named by
`docs/design/agent-launch-boundary-characterization.md`. The measured code revision contains the
reviewable `.4` Herdr lifecycle adapter and `.5` compatibility transition. The commit containing
this evidence necessarily adds this document, the checked closure update, and isolation tests on
top of that code revision: a Git commit cannot contain its own hash. Final exact-tip validation is
therefore recorded by the Beadhive group check/review attestation, not relabeled as a measurement
of the parent code revision.

## Tool and method record

| Tool | Version |
| --- | --- |
| Beadhive | `0.15.1+local.gfbc4537` |
| Git | 2.47.3 |
| uv | 0.12.1 |
| just | 1.57.0 |
| Python | CPython 3.13.5 for hermetic closures and coverage |
| pytest | 9.1.1 |
| coverage.py | 7.15.3, JSON format 3 |
| Ruff | 0.15.20 |
| RepoWise | 0.45.0 |

Line and static-complexity measurements parse the exact Git blobs with the standard-library AST.
"Decision nodes" is deliberately not presented as a third-party cyclomatic score: it counts
`If`, `For`, `AsyncFor`, `While`, `Try`, `ExceptHandler`, `IfExp`, `Match`, comprehension, and
`BoolOp` nodes. The reproducible shape of the command is:

```text
git ls-tree -r --name-only <revision> -- src/beadhive
git show <revision>:<python-path>
python -c '<ast.parse each selected blob; count lines, functions, classes, decision nodes>'
```

The selected surface is the legacy `herdr_plugin.py`/`herdr_views.py` pair plus every Python file
under `modules/agents` and `integrations/herdr`. Generated bytecode and tests are excluded.

## Source shape and complexity

| Surface | Before lines / functions / classes / decisions | After lines / functions / classes / decisions |
| --- | ---: | ---: |
| Legacy Herdr plugin + views | 6,190 / 164 / 9 / 1,376 | 2,442 / 65 / 1 / 486 |
| `modules/agents` | absent | 1,711 / 76 / 54 / 167 |
| `integrations/herdr` | absent | 7,189 / 246 / 81 / 1,341 |
| Combined selected surface | 6,190 / 164 / 9 / 1,376 | 11,342 / 387 / 136 / 1,994 |

The total grows because explicit versioned domain records, application services, ports, transport
types, topology parsing, and lifecycle evidence replace implicit contracts; total lines are not a
claim of code deletion. The legacy `herdr_plugin.py` drops from 3,836 to 87 lines: it now composes
the provider binding and preserves the import-compatible surface. The migrated orchestration lives
in `integrations/herdr/application_services.py`; `integrations/herdr/cli_application.py` is only
the module-object compatibility facade, while `integrations/herdr/cli.py` is the static thin Typer
projection over ten command-specific request/result use cases. The small `application_runtime.py`
control preserves direct-caller exception/output compatibility without owning Typer commands or
rendering, and `production_runtime.py` is the provider-effect runtime/resolver. Launch and spawn
cross prepare/commit/observe, fresh-generation launch recovery crosses observe/recover before any
provider effect, reap crosses teardown, and created-only compensation cannot close a concurrently
reused allocation. Exact consumers and removal gates remain in
`docs/design/herdr-compatibility-removal-ledger.md`. New core policy and provider lifecycle behavior
live behind the typed inward boundary; named typed use cases own command orchestration and the
facade remains compatibility debt, not a second provider-neutral contract.

## Import graph and fan-in

The checked standard-library collector was run with:

```text
UV_CACHE_DIR=/tmp/bh-agent-extraction-uv-cache just architecture-check
```

| Metric | Before | After |
| --- | ---: | ---: |
| Python modules | 193 | 216 |
| Internal import edges | 2,082 | 2,207 |
| Nonliteral dynamic-import sites | 2 | 2 |
| Owned cyclic SCCs | 6 | 6 |
| Modules in cyclic SCCs | 85 | 85 |
| Cyclic edges / imported symbols | 288 / 318 | 288 / 318 |

No SCC or cyclic edge was added. The new agent and Herdr integration files are outside every
cycle. Distinct in-repository importer fan-in is `herdr_plugin=2` before and after,
`integrations.herdr.agent_adapter=0→4`, `integrations.herdr.cli=0→1`,
`integrations.herdr.cli_application=0→1`, `integrations.herdr.application_services=0→2`,
`integrations.herdr.application_runtime=0→2`,
`modules.agents.application.services=0→1`, and `modules.agents.contracts.ports=0→2`. These are
static imports only; reflection, subprocesses, external packages, and downstream repositories are
not measured.

## Independent closures

`tests/closures.toml` now assigns the typed transport, lifecycle adapter, isolation sentinel,
legacy facade, presentation, and view tests to `plugin.herdr`. The registry remains fail-closed
and reports 14 present/6 absent closures. Direct, shared-contract, and reverse-dependent selectors
are de-duplicated by the checked closure runner.

| Closure | Direct / shared / reverse selectors | Collected | Executed | Pytest / wall |
| --- | ---: | ---: | ---: | ---: |
| `module.agents` | 3 / 1 / 2 | 292 | 292 passed | 29.62s pytest |
| `plugin.herdr` | 6 / 3 / 2 | 314 | 313 passed, 1 skipped | 32.19s pytest |

Commands:

```text
uv run python scripts/test_closures.py check
uv run python scripts/test_closures.py collect module.agents
uv run python scripts/test_closures.py collect plugin.herdr
just test-module agents
just test-plugin herdr
```

The agents import sentinel refuses Herdr, config, host, plugin, telemetry, Typer, process, network,
filesystem-config, and runtime-thread dependencies. The Herdr integration sentinel similarly
refuses root config, work/worktree, the legacy plugin, telemetry, Typer, provider-package imports,
plugin discovery, and ambient effects. All normal integration tests use fake command/socket or
typed lifecycle protocols without constructing the Beadhive runtime. The only real-provider smoke
is explicitly marked `integration`, requires `BH_TEST_HERDR_SESSION`, and otherwise skips before
constructing `HerdrClient`; that accounts for the one closure skip above.

## Coverage

The union of the two closures' agent, integration, compatibility, shared-contract, registry, CLI,
and reverse-dependent tests ran once with coverage on the selected production surface:

```text
uv run pytest -q <recorded closure selectors> \
  --cov=beadhive.modules.agents --cov=beadhive.integrations.herdr \
  --cov=beadhive.herdr_plugin --cov=beadhive.herdr_views \
  --cov-report=json:/tmp/bh-5wuc0-6-coverage.json
```

Result: 427 passed/1 skipped in 75.82s; 4,238 of 5,001 statements covered, 763 missing,
88 excluded, exact statement coverage **84.74305138972205%** (display 85%). Branch coverage and
per-test contexts are absent. Notable exact values are `modules/agents/domain/transaction.py`
360/380 (94.7368%), `modules/agents/application/services.py` 177/219 (80.8219%), and
`integrations/herdr/agent_adapter.py` 175/209 (83.7321%). The typed CLI projection is 100/102
(98.0392%), its named application services are 1,670/2,062 (80.9893%), the framework-neutral
outcome control is 46/50 (92%), and the top-level composition facade remains fully covered. The
characterized before selection reported a rounded 82% over four legacy modules; its different
source/selector set means it is a reference, not a like-for-like coverage delta.

RepoWise's wrapper reported `ok`, zero commits behind, and a 111.8 MiB index. Direct status detail
did not return within a bounded 20-second probe, and a worktree coverage query resolved the
34-repository workspace rather than a Beadhive per-test map. No index refresh or coverage ingest
was performed. RepoWise therefore contributes no dynamic per-test or selective-CI evidence here.

## Recent defect characterization and gates

The compatibility and managed-lifecycle closure contains 192 tests, all included in the green
427-pass/1-skip measured coverage union at the measured
code revision. It includes the frozen warm launch, reuse identity mismatch, stale generation,
partial allocation, start failure/duplicate race, restart recovery, and safe idempotent teardown
defects. It also proves reload compatibility, the deliberate owner migration, production provider
composition, ten command-specific typed request/result paths, semantic application outcomes with
CLI-only JSON/terminal/control translation, bound-port invocation before provider effects, bound
fresh-generation recovery, receipt-only runtime rehydration, and reuse-safe compensation. The
adapter suite separately covers prepare, commit, abort compensation, observation, recovery,
teardown, restart resolution, identity conflict, and portable redaction against fake provider
protocols.

The exact final evidence commit must pass, at one SHA:

```text
uv run pytest -q tests/unit/modules/agents tests/unit/integrations \
  tests/contracts/test_agent_launch_compatibility.py
UV_CACHE_DIR=/tmp/bh-agent-extraction-uv-cache just architecture-check
UV_CACHE_DIR=/tmp/bh-agent-extraction-uv-cache just check
```

The authoritative full gate is not replaced by either closure. Port, plugin, lifecycle, schema,
CLI compatibility, integration, and release-level results remain review/landing gates; no
selective-merge safety claim is made.

## `docs/MODULES.md` criteria

The reference extraction now demonstrates the applicable program criteria: domain/application
code has no Herdr or transport imports; Herdr implements typed launch/observation/recovery/teardown
ports without leaking provider identity into core records; its versioned manifest and isolated
tests remain checked; lifecycle outcomes and failure authority are explicit; the compatibility
facade has a consumer/removal ledger; no forbidden cycle was added; and both capability closures
run without root runtime fixtures. Operation-catalog, remaining transport composition, global SCC
reduction, all-module migration, and selective-test graduation remain later program gates. This
evidence does not claim the entire modular-architecture program is complete.

## Limitations and non-claims

- Static import and AST evidence does not observe reflection, runtime provider code, external
  repositories, or downstream packages.
- The opt-in real-provider smoke was skipped because no isolated test session was supplied; fake
  protocol fixtures exercise the normal closure deterministically.
- Coverage is statement-only, has no branch or per-test contexts, and has no selective-CI meaning.
- Native Claude Task and Codex collaboration children remain unmanaged; this extraction does not
  broaden provider or work authority.
- Herdr/Nix installation ownership and the existing git-workspace exception are outside scope.
- Full-suite and integration/release gates remain mandatory.
