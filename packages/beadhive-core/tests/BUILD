# Generator-shaped: see ../BUILD. Tests run in the Pants sandbox with only this package, the
# Beads client package and their fixtures (no stateful fixture plugin).
python_tests(
    name="tests",
    sources=["**/test_*.py"],
    tags=["category:test-only", "attest:packages"],
)
